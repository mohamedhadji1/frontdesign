import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Implementation CERT | Keystone",
  description: "Comprehensive readiness and cybersecurity implementation services, helping you deploy robust Computer Emergency Response Teams and response capabilities.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
